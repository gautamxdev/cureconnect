import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Us",
  description:
    "Contact Cure Connect and PHDC PRIVATE LIMITED for partnership inquiries and collaboration opportunities. Based in Nashik, Maharashtra.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
